# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Local, pinned external checkouts and datasets (kept out of the assistant repo;
# resolved from docs/pins.toml). See SPEC.md §4 — capabilities live upstream.
/external/
/vendor/
*.local.toml

# scripts/fetch_corpus.py's output (TD-4) and the hosted MkDocs backend's
# local telemetry sink (FR-11) -- both are runtime/deploy-time artifacts.
/corpus/
/data/

# Editor
.vscode/
.idea/
.DS_Store
